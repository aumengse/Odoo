<odoo>
  <data>
    <!-- explicit list view definition -->
    <record model="ir.ui.view" id="training_program_tree">
      <field name="name">training.program.tree</field>
      <field name="model">training.program</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="partner_id"/>
          <field name="employee_id"/>
        </tree>
      </field>
    </record>

    <record model="ir.ui.view" id="training_program_form">
      <field name="name">training.program.form</field>
      <field name="model">training.program</field>
      <field name="arch" type="xml">
        <form>
          <header>
            <field name="stage_id" widget="statusbar" options="{'clickable': '1'}"/>
          </header>
          <sheet>
            <div class="oe_title oe_inline">
              <label for="name"/>
              <h1><field name="name" placeholder="e.g. 0001"/></h1>
            </div>
            <group>
              <group>
                <field name="partner_id" options="{'no_create_edit': True}" required="1"/>
                <field name="employee_id" options="{'no_create_edit': True}"/>
              </group>
              <group>
                <field name="company_id" groups="base.group_multi_company" options="{'no_create_edit': True}"/>
                <field name="user_id" options="{'no_create_edit': True}" readonly="1"/>

                <label for="date_start" string="Training Dates"/>
                <div>
                  <field name="date_start" widget="daterange" options="{'related_end_date': 'date_end'}" class="oe_inline"/>
                  <span> to </span>
                  <field name="date_end" widget="daterange" options="{'related_start_date': 'date_start'}" class="oe_inline"/>
                </div>
              </group>
            </group>
          </sheet>
        </form>
      </field>
    </record>


    <!-- actions opening views on models -->
    <record id="action_training_program" model="ir.actions.act_window">
      <field name="name">Training Program</field>
      <field name="res_model">training.program</field>
      <field name="view_mode">tree,form</field>
    </record>

    <!-- Top menu item -->
    <menuitem name="Training Program"
              id="menu_root_training_program"
              web_icon="asec_customs,static/description/presentation.png"/>
    <!-- menu categories -->
    <menuitem name="Programs"
              id="menu_training_program_programs"
              parent="menu_root_training_program"
              action="action_training_program"
    />

    <!-- actions -->
    <!--
        <menuitem name="List" id="/opt/test/asec_customs.menu_1_list" parent="/opt/test/asec_customs.menu_1"
                  action="/opt/test/asec_customs.action_window"/>
        <menuitem name="Server to list" id="/opt/test/asec_customs" parent="/opt/test/asec_customs.menu_2"
                  action="/opt/test/asec_customs.action_server"/>
    -->
  </data>
</odoo>