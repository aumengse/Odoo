<odoo>
  <data>
    <!-- explicit list view definition -->
    <record model="ir.ui.view" id="member_contribution_tree">
      <field name="name">member.contribution.tree</field>
      <field name="model">member.contribution</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="date"/>
          <field name="amount"/>
          <field name="state"/>
        </tree>
      </field>
    </record>

    <record model="ir.ui.view" id="member_contribution_form">
      <field name="name">member.contribution.form</field>
      <field name="model">member.contribution</field>
      <field name="arch" type="xml">
        <form>
          <header>
             <field name="state" widget="statusbar"/>
          </header>
          <sheet>
            <group>
              <field name="name"/>
              <field name="date"/>
              <field name="amount"/>
            </group>
          </sheet>
        </form>
      </field>
    </record>

    <!-- actions opening views on models -->
    <record model="ir.actions.act_window" id="action_member_contribution">
      <field name="name">Member Contribution</field>
      <field name="res_model">member.contribution</field>
      <field name="view_mode">tree,form</field>
    </record>

    <!-- server action to the one above -->
    <!--
        <record model="ir.actions.server" id="osynx_loan.action_server">
          <field name="name">osynx_loan server</field>
          <field name="model_id" ref="model_osynx_loan_osynx_loan"/>
          <field name="state">code</field>
          <field name="code">
            action = {
              "type": "ir.actions.act_window",
              "view_mode": "tree,form",
              "res_model": model._name,
            }
          </field>
        </record>
    -->

    <!-- Top menu item -->

    <!-- menu categories -->
    <!--
        <menuitem name="Menu 1" id="osynx_loan.menu_1" parent="osynx_loan.menu_root"/>
        <menuitem name="Menu 2" id="osynx_loan.menu_2" parent="osynx_loan.menu_root"/>
    -->
    <!-- actions -->
    <!--
        <menuitem name="List" id="osynx_loan.menu_1_list" parent="osynx_loan.menu_1"
                  action="osynx_loan.action_window"/>
        <menuitem name="Server to list" id="osynx_loan" parent="osynx_loan.menu_2"
                  action="osynx_loan.action_server"/>
    -->
  </data>
</odoo>